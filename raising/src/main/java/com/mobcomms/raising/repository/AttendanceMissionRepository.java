package com.mobcomms.raising.repository;

import com.mobcomms.raising.entity.AttendanceMissionEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AttendanceMissionRepository extends JpaRepository<AttendanceMissionEntity, Long> {
}